# TradeVoice — Secure Vercel + Upstox

Private GitHub repo recommended. Never commit `.env` or an Upstox token.

Deploy: import this repo into Vercel, then Vercel Project Settings → Environment Variables → add `UPSTOX_ACCESS_TOKEN` with your Analytics Token. The browser does not receive the token.
